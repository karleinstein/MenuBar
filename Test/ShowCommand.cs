namespace LogicNP.ShellObjects
{
    using System;
    using System.ComponentModel;

    [Description("Specifies the state of menu item")]
    public enum ShowCommand
    {
        ShowMaximized = 3,
        ShowMinimized = 7,
        ShowNormal = 1
    }
}

